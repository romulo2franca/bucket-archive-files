---
env:
  PROJECT_NAME: bucket-archive-files
  REPO_URL: romulo2franca

tasks:
  verify:
    cmd: bin/verify.sh
  build:
    cmd: bin/build.sh
  publish:
    cmd: bin/publish.sh
  run:
    cmd: bin/run.sh
  deploy:
    cmd: bin/deploy.sh {{ .env | default "stage" }}
